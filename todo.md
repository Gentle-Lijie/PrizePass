# PrizePass 产品完善 TODO

> 视角：单管理员、轻量级比赛奖品兑换工具。以下建议用于提高真实活动中的可用性、运营效率、可靠性与上线信心，不改变现有核心业务模型。

## 产品目标

1. 管理员能在活动开始前确认配置完整，不因漏填、误操作或通知未配置影响发码。
2. 获奖人能在手机上顺利完成一次性多奖品兑换，失败时知道如何继续。
3. 现场工作人员能快速找到兑换单，并准确完成“已备货 → 已领取”。
4. 发生导入错误、通知失败、服务重启或误取消时，业务数据可恢复、问题可定位。

## 优先级定义

- **P0**：正式活动投入使用前完成；直接影响业务闭环或数据安全。
- **P1**：首轮真实活动后优先迭代；明显降低运营成本或用户困惑。
- **P2**：有稳定使用量后再做；先用实际反馈验证价值。

---

## P0：正式使用前

### 1. 比赛开启前检查

- [ ] 在管理员点击“开启比赛”前展示检查清单：截止时间仍在未来、至少有一个可兑换奖品、自提地点和说明完整。
- [ ] 显示 SMTP、运营通知邮箱、Webhook 的配置状态；未配置时明确说明会影响哪些通知。
- [ ] 对业务必需项采用阻止开启，对通知等可选配置采用二次确认，不悄悄失败。
- [ ] 开启成功后给出明确结果和兑换入口，而不是只改变状态文字。

**用户价值：** 避免活动已经开放后才发现没有奖品、截止时间错误或无法发码。

**验收标准：** 对每一种缺失配置都有可理解的提示；管理员可以准确知道哪些问题必须修复、哪些风险可以确认后继续。

### 2. 兑换状态与操作语义统一

- [ ] 列表和详情统一使用“已提交 / 待领取 / 已领取 / 已取消”四种状态文案。
- [ ] 操作按钮统一使用结果导向文案：“已备货”“已领取”“取消兑换”。
- [ ] “已备货”确认框说明操作后会通知获奖人；“已领取”确认框说明该状态不可撤销。
- [ ] “取消兑换”确认框展示将恢复的奖品数量，并说明兑换码会恢复使用。
- [ ] 所有状态按钮提交期间禁用，完成后只刷新受影响的兑换单，避免重复点击和页面闪烁。

**用户价值：** 现场人员清楚按钮代表“我要执行的动作”，还是“兑换单当前状态”。

**验收标准：** 列表与详情不存在同一动作不同叫法；终态没有可执行按钮；失败时原状态保持不变并显示原因。

### 3. 大数据量下仍可操作

- [ ] 奖品、获奖人、兑换记录、通知任务改为服务端分页，不一次加载全部数据。
- [ ] 获奖人支持按姓名、规范化邮箱、external_id、兑换码精确或前缀搜索。
- [ ] 兑换记录支持按兑换单号、联系人姓名、手机号搜索，并继续支持状态筛选。
- [ ] 通知任务支持按渠道、状态、事件类型筛选。
- [ ] URL 保存页码和筛选条件，查看详情返回后不丢失当前位置。

**用户价值：** 10,000 名获奖人的比赛也能快速找到目标记录，不因浏览器加载过多数据而卡顿。

**验收标准：** 10,000 名获奖人、10,000 条兑换记录下，首屏只返回当前页；搜索与翻页结果稳定且不重复、不遗漏。

### 4. 导入错误恢复体验

- [ ] 校验结果顶部按类型汇总错误数量，但仍保留逐行错误。
- [ ] 提供“下载错误明细 CSV”，字段包含原始行号、字段、原始值和错误说明。
- [ ] 错误表格支持只看错误行，并可快速定位到首个错误。
- [ ] 确认导入前再次显示人数、quota 合计以及“将创建多少个通知任务”。
- [ ] 大文件解析期间显示真实进行状态，禁止重复上传或重复确认。

**用户价值：** 管理员可以在表格中批量修复问题，而不是逐条复制页面提示。

**验收标准：** 任意无效导入仍保持全有或全无；下载的错误明细能准确对应原文件行号；修复后可直接重新上传。

### 5. 通知可达性与补救闭环

- [ ] 获奖人列表支持筛选 Email 或 Webhook 失败的记录。
- [ ] 对仍为 `issued` 的兑换码提供“重新发送通知”，只新建通知任务，不生成新码、不修改 quota。
- [ ] 最近任务页面展示下一次重试时间、最后一次尝试时间和清晰的失败原因。
- [ ] 支持勾选多个 `failed` 任务批量重新排队，操作前显示数量与渠道。
- [ ] 测试 Email/Webhook 后提供可见的状态变化，不要求管理员手工刷新。
- [ ] 对空的通知目标在业务发生前给出配置提示，避免创建注定失败且无法投递的任务。

**用户价值：** 发码或状态通知失败后，管理员能定位具体人员并安全补发。

**验收标准：** 补发不会改变兑换码；同一失败任务不会被并发重复发送；某一渠道失败不影响另一渠道。

### 6. 备份、恢复与上线检查

- [ ] 提供生产上线检查清单：HTTPS、数据库权限、上传目录权限、SMTP、Webhook、运营邮箱、PM2 状态、健康检查。
- [ ] 提供 MySQL 与 `UPLOAD_DIR` 同时间点备份脚本或明确的运维命令。
- [ ] 提供恢复演练步骤，并至少完成一次从空环境恢复到可查看图片和兑换记录。
- [ ] `deploy.sh` 在重载前验证生产构建、数据库连接、迁移状态和上传目录，但不删除业务数据。
- [ ] 明确日志轮转、磁盘空间检查和故障时的回滚步骤。

**用户价值：** 服务器故障或部署失败不会导致兑换记录与奖品图片永久丢失。

**验收标准：** 能从一次完整备份恢复 MySQL 与图片；恢复后随机抽查奖品图片、兑换码状态、兑换快照和通知任务一致。

### 7. 自动化发布门禁

- [ ] 每次合并前自动运行后端 MySQL 测试、ESLint、TypeScript 检查和生产构建。
- [ ] 增加前端组件测试：管理员密码入口、购物篮额度计算、状态按钮显示、导入错误呈现。
- [ ] 增加浏览器 E2E：创建比赛 → 创建奖品 → 导入获奖人 → 使用兑换码 → 多奖品兑换 → 已备货 → 已领取。
- [ ] 增加取消后库存和兑换码恢复、再兑换成功的 E2E。
- [ ] E2E 使用独立测试库和临时上传目录，结束后自动清理。

**用户价值：** 后续小改动不会悄悄破坏核心兑换流程。

**验收标准：** 任一门禁失败时不可发布；测试不会连接或修改生产数据库。

---

## P1：首轮活动后优先迭代

### 8. 现场领取效率

- [ ] 兑换记录页将搜索框默认聚焦，支持按 Enter 搜索。
- [ ] 详情中提供一键复制兑换单号和手机号。
- [ ] 搜索结果只有一条时支持直接打开详情，但不自动改变状态。
- [ ] 已领取操作前再次显示联系人、奖品摘要和数量，减少拿错奖品。
- [ ] 优化平板和窄屏布局，现场不依赖横向滚动才能看到操作按钮。

**用户价值：** 高峰期能更快核对并完成领取，同时降低误操作。

### 9. 兑换码停用与恢复

- [ ] 在获奖人列表中为 `issued` 兑换码提供“停用”。
- [ ] 为 `disabled` 兑换码提供“恢复使用”。
- [ ] 已兑换的码不能手工恢复，只有取消对应兑换时才能恢复。
- [ ] 停用或恢复操作必须二次确认，并清楚展示获奖人和兑换码尾号。

**用户价值：** 邮箱填错、资格撤回或兑换码误发时，可以阻止继续兑换，而无需删除业务记录。

**验收标准：** 严格遵守 `issued ↔ disabled`；不产生新兑换码；公开接口统一拒绝 disabled 兑换码。

### 10. 奖品与图片容错

- [ ] 外链图片加载失败时显示统一占位图和奖品名称，不出现破损图片图标。
- [ ] 图片使用懒加载，并为上传图片保留合理尺寸，改善移动端首屏速度。
- [ ] 库存为 0 的奖品在后台突出显示“已兑完”。
- [ ] 编辑奖品时明确说明历史兑换快照不会随之改变。
- [ ] 删除奖品失败时说明是因为已有兑换引用，并提供查看相关兑换的入口。

**用户价值：** 图片源失效或奖品售罄不会让兑换页面显得损坏，管理员也能理解历史快照规则。

### 11. 表单防误操作

- [ ] 比赛、奖品、通知模板有未保存修改时，离开页面前提示。
- [ ] 保存按钮仅在内容发生变化且校验通过时启用。
- [ ] 长任务提供明确的 loading、成功和失败状态，避免用户重复点击。
- [ ] API 失败提示区分“输入错误、状态冲突、网络中断、服务器错误”，并提供安全的重试入口。

**用户价值：** 减少因刷新、返回或网络抖动造成的重复操作和内容丢失。

### 12. 截止时间与时区表达

- [ ] 管理后台明确显示当前浏览器时区，并在保存截止时间前展示完整时间。
- [ ] 兑换页显示“截止日期 + 时间”，临近截止时给出醒目提示。
- [ ] 比赛超过截止时间后，在后台显示“已过截止时间”的派生提示，但不新增数据库状态。
- [ ] 所有列表、详情、导出对同一时间使用一致格式，并注明导出为 UTC 或本地时间。

**用户价值：** 跨时区部署或浏览器设置不同时，不会误判兑换截止时间。

### 13. 无障碍与移动端体验

- [ ] 所有表单控件具有可读 label，弹窗支持 Esc 关闭与焦点回收。
- [ ] 状态不能只靠颜色区分；按钮、错误和成功提示满足对比度要求。
- [ ] 兑换流程可完全使用键盘操作，并对屏幕阅读器播报额度变化和提交结果。
- [ ] 覆盖常见手机宽度，确保数量按钮、确认信息和提交按钮不会遮挡。
- [ ] 图片提供有意义的替代文本。

**用户价值：** 更多获奖人能独立完成兑换，也减少移动端操作失败。

---

## P2：有稳定使用量后再评估

### 14. 安全的批量操作

- [ ] 评估是否需要批量“已备货”；仅允许选择 `submitted` 兑换单。
- [ ] 操作前展示兑换单数量和奖品总件数，确认后逐条返回成功或失败结果。
- [ ] 不提供批量“已领取”，避免现场误把未到场人员标记为已领取。

**启动条件：** 单场比赛出现大量集中备货，逐单操作已成为明确瓶颈。

### 15. 比赛配置复用

- [ ] 评估“复制比赛设置与奖品”功能，只复制配置和奖品字段，不复制获奖人、兑换码、库存消耗、兑换记录或通知任务。
- [ ] 新比赛始终为 `draft`，截止时间必须重新填写并确认库存。

**启动条件：** 同一运营团队重复举办结构相似的比赛，并能证明手工录入成本较高。

### 16. 操作体验持续优化

- [ ] 每场真实活动后记录管理员遇到的前三个阻塞点和获奖人最常见的失败原因。
- [ ] 根据实际问题调整文案、默认排序、搜索字段和确认步骤。
- [ ] 仅在有明确证据时增加新配置项，避免把轻量工具演变为通用平台。

---

## 产品效果衡量

这些指标用于活动复盘或验收抽样，不要求在产品内建设报表系统：

- 获奖人从打开兑换页到提交成功的中位耗时。
- 首次输入兑换码后的成功提交比例。
- 因表格格式问题导致的导入重试次数。
- `code_issued` Email 与 Webhook 最终发送成功比例。
- 现场从搜索兑换单到标记已领取的平均耗时。
- 因误操作发生的取消、重复处理或人工数据修复次数。
- 每场活动需要人工补发兑换通知的人数。

## 明确不纳入本路线图

- 多管理员、管理员账号体系、角色权限和 RBAC。
- 报表中心、仪表盘、审计日志或复杂统计系统。
- 多自提点、配送、地址、物流、支付、退款或补差价。
- 余额账户、分次消费、未用 quota 保留。
- 仓库、库存预占、库存批次、序列号或库存流水。
- 商品网页抓取、价格同步、库存同步或第三方业务数据同步。
- Redis、Celery、RabbitMQ、微服务或其他尚无实际负载依据的基础设施。

## 建议实施顺序

1. 先完成 P0-1、P0-2、P0-5，降低首场活动的直接业务风险。
2. 接着完成 P0-3、P0-4，确保大名单和错误数据仍可运营。
3. 完成 P0-6、P0-7 后再进入正式生产环境。
4. 首场活动结束后，用现场问题决定 P1 的实际顺序。
5. P2 必须满足对应启动条件，不提前建设。
